<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rooms</title>
</head>
<body>
    <h1>Rooms</h1>
    <% if (message) { %>
        <p><%= message %></p>
    <% } else { %>
        <ul>
            <% rooms.forEach(room => { %>
                <li><%= room.nomor_kamar %> - <%= room.tipe_kamar %> - <%= room.harga %></li>
            <% }); %>
        </ul>
    <% } %>
</body>
</html>