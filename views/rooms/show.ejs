<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container mt-5">
        <h1>Room Details</h1>
        <% if (room) { %>
            <p>Room Number: <%= room.nomor_kamar %></p>
            <p>Price: <%= room.harga %></p>

            <h2>Users Renting this Room:</h2>
            <% if (room.status && room.status.length > 0) { %>
                <ul>
                    <% room.status.forEach(status => { %>
                        <li>
                            User: <%= status.nik ? status.nik.name : "User not found" %> (<%= status.nik ? status.nik.email : "Email not found" %>)<br>
                            Status: <%= status.status %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No users are currently renting this room.</p>
            <% } %>
        <% } else { %>
            <p>Room not found.</p>
        <% } %>

        <a href="/rooms" class="btn btn-primary mt-4">Back to Rooms List</a>
    </div>
    </body>
</html>
